export const environment = {
  apiBaseUrl: '/api',
  pollingIntervalMs: 5000,
  staleThresholdMs: 15000,
  backtestHistorySize: 5,
  confirmFlattenWord: 'FLATTEN',
  useMocks: true,  // Enabled by default so UI works out of the box
};
